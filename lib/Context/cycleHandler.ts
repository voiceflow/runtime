import Context, {ActionType} from "@/lib/Context";

const HANDLER_OVERFLOW = 400;

const cycleHandler = async <T, S, V, STT, STV, STS>(context: Context<T, S, V, STT, STV, STS>, diagram): Promise<void> => {
    let lineID: string = null;
    let nextID: string = null;
    let i: number = 0;
    while(line) {
        if (i > DO_WHILE_LIMIT) {
            line = false;
            break;
        }
        i++;
    }
};

export default cycleHandler;